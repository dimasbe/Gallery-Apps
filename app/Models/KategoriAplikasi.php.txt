<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class KategoriAplikasi extends Model
{
    use HasFactory;

    protected $table = 'kategori_aplikasi';
    protected $fillable = [
        'sub_kategori', 
        'nama_kategori', 
        'gambar_kategori_aplikasi',
    ];

    // Disable Laravel's default timestamps if you want to use your custom ones
    public $timestamps = false;

    // Mutators for custom timestamp columns if needed, though useCurrent() and useCurrentOnUpdate() in migration handle this well
    // public function setTanggalDibuatAttribute($value)
    // {
    //     $this->attributes['tanggal_dibuat'] = now();
    // }

    // public function setTanggalDieditAttribute($value)
    // {
    //     $this->attributes['tanggal_diedit'] = now();
    // }

    public function aplikasi()
    {
        return $this->hasMany(Aplikasi::class, 'id_kategori', 'id_kategori');
    }
}
